package com.cashkaro.cashkaro.utils;

import android.support.v4.app.Fragment;

public interface FragmentChangeListener {
     void addFragment(Fragment fragment);
    void replaceFragment(Fragment fragment);

}